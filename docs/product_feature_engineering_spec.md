| Feature                         | Loại                           | Kiểu           | Miền giá trị / Valid values                                                     | Insight                                                                                                                                                                                                     | Cách thực hiện                                                                                                                                                                                                                               | Ý nghĩa                                                                                                                                                                               |
| ------------------------------- | ------------------------------ | -------------- | ------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `record_gap_months`             | Chất lượng lịch sử             | Integer / NULL | `[1, +∞)`; NULL nếu chưa có record trước                                        | EDA cho thấy lịch sử customer không phải lúc nào cũng liên tục; hai record liên tiếp trong dữ liệu có thể cách nhau nhiều tháng.                                                                            | Với từng `ncodpers`, sort theo `fecha_dato`, tính số tháng giữa record hiện tại và record gần nhất trước đó.                                                                                                                                 | Cho model biết độ liên tục của history; phân biệt lịch sử tháng liền kề với lịch sử bị gián đoạn. Đồng thời dùng để xác định transition nào đủ điều kiện tính acquisition/drop.       |
| `customer_history_length`       | Chất lượng lịch sử             | Integer        | `[0, +∞)`                                                                       | Phân phối số checkpoint/customer không đồng đều; một lượng lớn customer có 17 records nhưng cũng có nhóm lớn chỉ có khoảng 11 records do bắt đầu xuất hiện muộn.                                            | Tại sample `(customer, t)`, đếm số historical records của customer có `fecha_dato < t`. Cần xem kết hợp với `antiguedad` vì đây là **độ dài lịch sử quan sát được trong dataset**, không phải thời gian thực tế customer đã ở với ngân hàng. | Cho model biết các historical/RFM features của customer được xây dựng từ bao nhiêu dữ liệu lịch sử; cùng một giá trị RFM có thể có độ tin cậy khác nhau nếu history length khác nhau. |
| `products_owned_count`          | Portfolio                      | Integer        | `[0, 24]`                                                                       | EDA portfolio depth cho thấy future acquisition rate thay đổi theo số lượng product customer đang sở hữu.                                                                                                   | Tại sample ở `t`, tính tổng trạng thái của 24 product tại `t-1`: `Σ product_state_(t-1)`.                                                                                                                                                    | Biểu diễn độ sâu/rộng của portfolio hiện tại; phân biệt customer đang có ít hay nhiều quan hệ sản phẩm với ngân hàng.                                                                 |
| `cumulative_acquisitions`       | Frequency                      | Integer        | `[0, +∞)`                                                                       | Product event EDA cho thấy customer có mức độ hoạt động mua mới khác nhau; chỉ nhìn portfolio hiện tại không phản ánh customer đã acquire bao nhiêu lần trong lịch sử.                                      | Xác định acquisition khi product chuyển `0 → 1` trên transition có `record_gap_months = 1`; cộng toàn bộ acquisition events của customer trước `t`.                                                                                          | Biểu diễn **tần suất mua mới tích lũy** của customer trong phần lịch sử quan sát được.                                                                                                |
| `months_since_last_acquisition` | Recency                        | Integer / NULL | `[1, +∞)` nếu từng có acquisition; NULL nếu chưa từng quan sát thấy acquisition | EDA theo recency cho thấy future acquisition rate khác nhau rõ rệt tùy số tháng kể từ acquisition gần nhất; nhóm chưa từng acquire cũng có behavior khác.                                                   | Tìm acquisition event gần `t` nhất trong lịch sử hợp lệ và tính khoảng cách theo tháng từ event đó đến `t`. Nếu chưa từng quan sát acquisition thì để NULL.                                                                                  | Biểu diễn **độ gần của hành vi mua mới gần nhất**: customer vừa hoạt động gần đây hay đã lâu không mua thêm sản phẩm.                                                                 |
| `never_acquired_before`         | Recency                        | Boolean        | `{0, 1}`: `1` = chưa từng quan sát acquisition; `0` = đã từng có                | EDA cho thấy nhóm chưa từng quan sát acquisition không nên bị gộp với một giá trị recency thông thường.                                                                                                     | Đặt `1` nếu không tồn tại acquisition event hợp lệ trước `t`, ngược lại `0`. Sử dụng cùng `months_since_last_acquisition`.                                                                                                                   | Giúp model phân biệt **“không có recency vì chưa từng acquire”** với customer đã từng acquire nhưng lần gần nhất xảy ra từ lâu.                                                       |
| `acquisitions_last_1m`          | Hành vi gần đây                | Integer        | `[0, +∞)`                                                                       | EDA rolling acquisition cho thấy recent acquisition activity có thể chứa signal về hành vi tiếp theo. Window ngắn phản ánh hành vi rất gần hiện tại.                                                        | Đếm tổng số `0 → 1` acquisition events của tất cả product trong **1 tháng lịch sử gần nhất trước `t`**; chỉ dùng transition hợp lệ.                                                                                                          | Đo cường độ mua mới **rất gần đây** của customer.                                                                                                                                     |
| `acquisitions_last_3m`          | Hành vi gần đây                | Integer        | `[0, +∞)`                                                                       | EDA rolling windows cho thấy các window có tương quan nhưng chưa hoàn toàn trùng lặp; 3 tháng cung cấp góc nhìn short-term rộng hơn 1 tháng.                                                                | Đếm tổng acquisition events của customer trong **3 tháng trước `t`**.                                                                                                                                                                        | Biểu diễn mức độ hoạt động mua mới ngắn hạn, ít nhạy với một tháng đơn lẻ hơn `acquisitions_last_1m`.                                                                                 |
| `acquisitions_last_6m`          | Hành vi gần đây                | Integer        | `[0, +∞)`                                                                       | `acquisitions_last_3m` và `acquisitions_last_6m` có tương quan đáng kể nhưng không hoàn toàn giống nhau, nên EDA chưa đủ cơ sở để loại một trong hai.                                                       | Đếm tổng acquisition events của customer trong **6 tháng trước `t`**.                                                                                                                                                                        | Biểu diễn xu hướng mua mới trung hạn; giúp phân biệt customer chỉ vừa hoạt động gần đây với customer duy trì hoạt động trong khoảng thời gian dài hơn.                                |
| `cumulative_drops`              | Frequency / Portfolio movement | Integer        | `[0, +∞)`                                                                       | EDA acquisition/drop cho thấy chỉ nhìn portfolio hiện tại không phản ánh được customer đã từng rời bỏ bao nhiêu product. Hai customer có cùng portfolio hiện tại có thể có lịch sử biến động rất khác nhau. | Xác định drop khi product chuyển `1 → 0` trên transition có `record_gap_months = 1`; cộng tổng số drop events của tất cả product trước `t`.                                                                                                  | Biểu diễn **tổng mức độ thu hẹp/biến động portfolio trong lịch sử**; bổ sung chiều hành vi ngược lại với `cumulative_acquisitions`.                                                   |
